# Auditoría — Iteración 3: Adjuntar (VAL-58, VAL-59, VAL-60; VAL-57 recortada)

# VEREDICTO: NO PUBLICAR — 70 / 100

**Fecha:** 14-15 de septiembre de 2026 · **Escribe:** auditor · **Alcance:** HEAD `f1dabd7`
(`Cerrar los hallazgos H1, H2 y H5 de QA`) en `claude/travel-planning-app-mvp-jag9be`, que es el
commit especificado para esta auditoría. Incluye por transitividad los commits que integraron la
iteración (`4479801`, `85f8815`, `19373a1`, `f1dabd7`).

**Aviso de proceso, no de puntaje:** mientras esta auditoría corría, apareció en el árbol de trabajo
—y después se commiteó como `58ca080`, un commit posterior a `f1dabd7`— exactamente el arreglo
documental que este informe pide como lo más urgente (ver dimensión 3 y 6, y la nota al final). No lo
uso para puntuar `f1dabd7`, porque el encargo fue auditar ese commit puntual y `58ca080` no estaba
escrito cuando arrancó la tarea. Lo dejo trazado porque es información real y porque confirma, con
fecha y hash, que el hallazgo es correcto y ya tiene remedio esperando revisión.

El trabajo de ingeniería de esta entrega es sólido donde se lo puede medir: los tres motores siguen
byte a byte idénticos a su fuente en `app/parts/` (lo comprobé yo, no repetí el número), las 66
aserciones de navegador que más importan corren en verde con gestos reales y con controles negativos
que de verdad fallan cuando tienen que fallar, y el arreglo de H5 es el mejor diagnóstico de causa
raíz de las tres correcciones. Pero la iteración se cierra recortando su propio criterio de éxito
(VAL-57, "la prueba que define el éxito" según el propio brief) y, al llegar a `f1dabd7`, **esa
decisión no está escrita en ningún documento del proyecto** — ni en el brief, ni en el roadmap, ni en
el backlog. Vive sólo en la cabeza de quien la tomó y en lo que me contaron para esta tarea. Es
exactamente el escenario que el PM pidió verificar, y la respuesta es que no, no estaba.

---

## Tabla de las seis dimensiones

| # | Dimensión | Peso | Puntaje | Por qué |
|---|---|---|---|---|
| 1 | Verificación en el entorno real | 30 | **20** | Sustituto fiel (Chromium/Playwright, gestos reales, sin disparo de eventos internos), con la brecha declarada sin excepción en cada commit y en `LEEME.md`. Nunca corrió en el Artifact publicado ni en el teléfono, y eso también está dicho sin rodeos. Banda "20" de la rúbrica, ni más ni menos. |
| 2 | Diagnóstico de causa raíz | 15 | **13** | H1 y H5 están reproducidos, diagnosticados con precisión y probados con arreglo-elimina-la-falla (lo repetí yo mismo para H5, ver abajo). H1 además cubrió una segunda hipótesis (el cuerpo se escribe antes que el ítem) que nadie pidió pero que es exactamente el patrón "dos causas, un síntoma" que exige el proyecto. Resta un poco: H2 es una decisión de producto, no una causa de bug, y quedó bien declarada pero no auditada como causa; y la ambigüedad sustituto-vs-teléfono de H5 queda correctamente señalada pero sin ningún paso concreto asignado a nadie para cerrarla en el teléfono. |
| 3 | Honestidad de lo verificado | 15 | **9** | Los commits y el reporte de QA son un ejemplo de honestidad — separan verificado/no comprobado sin que nadie lo pida. Pero el **documento contrato** (`docs/briefs/adjuntar.md`) al llegar a `f1dabd7` sigue diciendo "Estado: en definición" y no menciona el recorte de VAL-57 ni la decisión sobre H4, y el roadmap describe un riesgo ("si no baja la librería, VAL-57 no existe") que ya se había resuelto dos días antes. Y hay una afirmación explícita sin cumplir: el brief dice "Se arregla en esta iteración" sobre las esperas fijas de `tier-del-modelo.js`, y no se arregló (verificado leyendo el código, ver abajo) — nadie la retractó. |
| 4 | Cumplimiento del contrato | 15 | **12** | VAL-58 y VAL-59 cumplidos punto por punto contra el brief, con una excepción menor y declarada (H4, cupo total de la base). VAL-60 cumplido en lo que depende de la app, con la brecha de plataforma (`modelTierApplied`) declarada y verificada como imposible de resolver desde acá. VAL-57 es P0 y, leído tal cual está escrito en el brief a `f1dabd7`, no está cumplido — un criterio no verificado cuenta como no cumplido, dice la rúbrica, y acá ni siquiera está reconocido como recortado en el propio contrato. |
| 5 | Calidad interna | 15 | **12** | Motores embebidos idénticos a `app/parts/` (comprobado byte a byte por mí, no sólo por el mismo test suite). Las dos aserciones "que no podían fallar" que el PM señaló (el aviso de 1500 ms y la etiqueta de cuenta) están corregidas en `f1dabd7`, y la del aviso la reproduje con control negativo real. Resta por la deuda de prueba de `tier-del-modelo.js` que el brief da por resuelta y no lo está, y porque no repliqué el control negativo de la carrera de `adjuntar-documento.js` (es una condición de carrera, difícil de forzar de forma determinística; lo declaro sin verificar, no lo doy por bueno ni por malo). |
| 6 | Diseño y decisiones de producto | 10 | **4** | La decisión de cerrar sin VAL-57 es defendible — publicar dos mejoras probadas en vez de sentarse sobre ellas es un argumento razonable — pero en `f1dabd7` está **escondida**, no declarada: no aparece en el brief, ni en el roadmap, ni en el backlog. Lo mismo con H4: el brief la nombra como "hallazgo abierto a propósito" recién en `58ca080`, posterior al commit auditado. La rúbrica es explícita: una decisión que contradice el brief resta si está escondida. |
| | **Total** | **100** | **70** | |

**Diferencia con la autopuntuación:** no vi ninguna autopuntuación entregada junto con `f1dabd7` para
comparar (no encontré un reporte de entrega con tabla propia en `docs/`). Si existe en otro lugar y no
la vi, no pude contrastarla.

---

## Lo que verifiqué, con el comando o archivo, y el resultado

### Pruebas corridas por mí, de a una, con margen de 560 s

| Arnés | Aserciones reportadas | Resultado real | Comando |
|---|---|---|---|
| `motores-desde-html.js` | 68+101+39 = 208 | **208 en verde**, confirmado | `NODE_PATH=/opt/node22/lib/node_modules node app/pruebas/motores-desde-html.js` |
| `importar-botones.js` (normal) | 11→12 | **12 en verde**, confirmado | ídem con ruta del HTML |
| `importar-botones.js` (`bloquear`) | 12 | **12 en verde**, confirmado — el aviso real de "selector mudo" sigue apareciendo cuando el selector de verdad no abre | ídem + `bloquear` |
| `adjuntar-documento.js` | 81 | **81 en verde**, confirmado | ídem |
| `tarjeta-y-lote.js` | 77 | **77 en verde**, confirmado | ídem |
| `cruce-adjunto-huerfano.js` (H1, de QA) | 4 (antes: 2 fallas) | **4 en verde**, confirmado — el arnés de QA no se tocó | ídem |
| `cruce-archivo-dos-reservas.js` (H2, de QA) | 4 | **4 en verde**, confirmado — el cierre ahora nombra "Vuelo G3 1245 se guardó con todos sus datos, pero sin el comprobante" | ídem |
| `cruce-huerfano-al-fallar.js` (H1, segunda puerta, nuevo) | 5 | **5 en verde**, confirmado | ídem |
| `tier-del-modelo.js` | 42 | **42 en verde**, 5 corridas seguidas sin intermitencia (máquina descargada) | ídem |
| `importar-arranque.js` | 59 | **59 en verde**, confirmado | ídem |
| `importar-sin-imagenes.js` | 45 | **45 en verde**, confirmado | ídem |

**No corrí** `valija-bloque-b.js` (111) ni `hallazgos-qa-bloque-b.js` (31): son del bloque B de la
iteración 2, no tocado por esta entrega, y prioricé el presupuesto de tiempo en lo nuevo. Lo declaro
como no verificado por mí en esta auditoría, no lo doy por bueno.

### Controles negativos: los corrí y de verdad fallan

- **El más importante, porque es el que el PM escribió con su propio arreglo (H5):** reconstruí a mano
  la guarda vieja (`!(input.files && input.files.length)` en vez de la bandera `llegaron`) en una copia
  de `valija.html`, y corrí `importar-botones.js` contra esa copia. **Falló exactamente la aserción que
  tiene que fallar** ("un archivo que SÍ llegó no dispara el aviso de selector mudo 1500 ms después"),
  y sólo esa. Contra el HTML real, la misma aserción pasa. Esto **no es una prueba reescrita para
  quedar en verde**: mide un comportamiento real, distingue el build bueno del malo, y lo comprobé yo,
  no lo creí.
- `sed 's/texto:"default"/texto:"complex"/'` → 5 fallas (documentado: 5). Confirmado.
- `sed 's/imagenes:"complex"/imagenes:"default"/'` → 1 falla (documentado: 1). Confirmado.
- `sed 's/equipaje:"complex"/equipaje:"quick"/'` → 2 fallas (documentado: 2). Confirmado.
- `sed 's/maxDocumentos: 6/maxDocumentos: 1/'` sobre `tarjeta-y-lote.js` → 3 fallas, incluida "UNA
  llamada al modelo para los tres documentos (fueron 3)". Confirma que VAL-60 es real, no un espejismo
  del simulador.
- `sed` sobre el barrido de huérfanos en `cruce-huerfano-al-fallar.js` → 1 falla, la que tiene que
  fallar. Confirmado.

### El motor embebido, comparado byte a byte (no de memoria, no "los tests dan igual")

Extraje el cuerpo del IIFE de `AdjuntosEngine` y de `ImportEngine` de `app/valija.html` y lo comparé
con `app/parts/adjuntos-engine.js` y `app/parts/import-engine.js` quitando sólo el envoltorio UMD.
Diferencia: una línea (`"use strict";` duplicada, inocua) en los dos casos. **Idénticos.** No repetí
esto para `PackingEngine` con la misma técnica porque mi propio script de extracción cortó mal el
cierre del módulo (no es del código, es mi comando); el propio `motores-desde-html.js` del repo, que
usa un extractor más cuidadoso, corrió las 68 pruebas de `PackingEngine` contra la copia embebida y
dieron 68 en verde, así que no lo marco como sospechoso — lo marco como no re-verificado por mí con mi
propio método, distinto de "verificado".

### La afirmación de "192 KB → 190 KB" y de "196.608 bytes"

`grep -n "192 KB\|190 KB" app/valija.html` — sólo aparece "190 KB", en los lugares que QA ya había
contado (C4 del reporte de QA). No encontré ningún "192 KB" suelto. Coincide con lo declarado.

### VAL-57: confirmo que sigue sin construirse (H3 de QA sigue abierto)

`grep -n "imp-more" app/valija.html` y lectura de las líneas 686-695 y 8937: el `<summary>Pegar el
texto en cambio</summary>` sigue dentro de un `<details class="imp-more">` que sólo se abre solo
cuando la vista ya está degradada (sin imágenes o sin PDF). En el camino normal, sigue siendo la vía
de escape colapsada — exactamente lo que el brief pide sacar y exactamente lo que QA encontró. **No
cambió entre el reporte de QA y `f1dabd7`**, porque `f1dabd7` sólo cerró H1, H2 y H5, no H3 — y eso es
correcto, dado que la decisión fue no construir VAL-57 en esta iteración. El problema no es que falte
el código: es que el brief no dice que falta a propósito.

### La deuda de prueba de `tier-del-modelo.js`: NO se arregló, y el brief dice que sí

El brief (`docs/briefs/adjuntar.md`, sección VAL-60) dice, textual: *"tres esperas fijas después de
tocar `#pk-build` y `#save`... Se arregla en esta iteración."* `grep -n "waitForTimeout" app/pruebas/tier-del-modelo.js`
muestra que las esperas fijas después de `#pk-build` (líneas 303, 318, 421) y `#save` (línea 330)
**siguen ahí, sin cambios**, y `git log --oneline -- app/pruebas/tier-del-modelo.js` muestra que el
archivo no se tocó desde el 12/09 salvo un cambio de comentario. Corrí el arnés 5 veces seguidas y no
reproduje la falla (coincide con lo que ya se sabía: sólo aparece con la máquina cargada), pero **la
causa que el brief prometió eliminar sigue en el código**. Esto es una afirmación del contrato que no
se cumplió y que nadie retractó — el patrón exacto de "la documentación que quedó mintiendo".

### La documentación del recorte de VAL-57 y de H4, al commit auditado

```
git show f1dabd7:docs/briefs/adjuntar.md | head -10
```
→ `**Estado:** en definición`, sin mención de VAL-57 recortada, sin mención de H4.

```
git show f1dabd7:docs/roadmap.md | sed -n '95,111p'
```
→ Sigue diciendo "Alcance: VAL-57 a VAL-60" sin distinguir qué se entregó, y describe el riesgo del
CDN de reconocimiento como si todavía decidiera la iteración, cuando el paso cero ya lo había resuelto
(negativamente) el 12/09, dos días antes de este commit.

```
grep -n "VAL-57\|VAL-58\|VAL-59\|VAL-60" docs/backlog.md  (contra f1dabd7)
```
→ Cero resultados. El backlog nunca tuvo estas historias, así que tampoco puede reflejar el recorte.

**Este es el hallazgo central de la auditoría**, y es exactamente lo que se me pidió comprobar: la
decisión de cerrar sin VAL-57, y la de dejar H4 abierto a propósito, existían el 14/09 — pero, al
commit que se me pidió auditar, **no estaban escritas en ningún documento que sobreviva a la memoria
de quien las tomó.**

### El commit que apareció durante la auditoría

Mientras corría estas verificaciones, `git status` pasó de "working tree clean" (con HEAD en
`f1dabd7`) a mostrar cambios sin commitear en exactamente `docs/briefs/adjuntar.md` y
`docs/roadmap.md` — y después esos cambios se commitearon como `58ca080` ("Cerrar la iteración 3 en la
documentación, con el recorte escrito"), que además toca `docs/backlog.md` agregando VAL-57 (con sus
criterios) y VAL-62 (el hallazgo H4) como historias del backlog, y corrige un corrimiento de
numeración entre iteración 4 y 5 que venía de antes. Leí el diff completo: es honesto, específico,
declara la recomendación en contra del PO, y usa rutas de archivo exactas (no `docs/` completo), que es
justamente la regla de coordinación que en este proyecto se rompió dos veces. **No lo cuento para el
puntaje de `f1dabd7`** porque no es el commit que se me pidió auditar y porque yo no lo audité como
entrega propia (no corrí pruebas contra él, no lo crucé con QA). Pero es la prueba más directa posible
de que el hallazgo de esta auditoría es real: alguien lo vio y lo arregló apenas se señaló.

---

## Lo que falta para llegar a 85, en orden

1. **Que el commit que de verdad se publique incluya la documentación del recorte**, no que quede en un
   commit posterior sin auditar. Si `58ca080` es el candidato, hay que auditarlo como tal — es rápido,
   toca sólo tres archivos de documentación y no cambia `valija.html` ni las pruebas, así que no hace
   falta repetir la batería completa de pruebas de navegador, sólo confirmar que el texto nuevo es
   preciso (yo ya lo leí y me pareció correcto, pero no es mi auditoría formal de ese commit).
2. **Retractar o cumplir la promesa de `tier-del-modelo.js`.** El brief dice que se arregló; no es
   cierto. O se cambian las esperas fijas por una señal real de que el recálculo terminó, o se corrige
   el texto del brief para que diga "sigue pendiente" — cualquiera de las dos cierra la honestidad, pero
   una de las dos tiene que pasar.
3. **Cerrar la ambigüedad de H5 con un paso concreto asignado**, no sólo declarada. Ver la sección de
   abajo.
4. **Decidir explícitamente si H3 (VAL-57 sin construir) bloquea la publicación de VAL-58/59/60**, y que
   esa decisión —ya tomada, según se me dijo— quede en el mismo lugar que las demás: el brief, no sólo
   en un mensaje de tarea.
5. Nada de la iteración corrió todavía en el Artifact publicado ni en el teléfono. Eso no es un défcit
   de esta entrega en particular —es el límite de este entorno—, pero es la única forma de subir la
   dimensión 1 por encima de 20 y de poder decir "terminado" en el sentido de `CLAUDE.md`, no sólo
   "candidato".

---

## Lo que nadie puede verificar desde acá, y los pasos exactos para el PM

Esta sección no queda vacía: hay bastante, y es justo lo más importante.

1. **H5, la duda genuina que QA dejó abierta y que `f1dabd7` no puede cerrar desde este entorno.**
   Pasos para el PM, en su teléfono, sobre el Artifact publicado:
   - Abrir una reserva (nueva o existente), tocar "Archivo" en el bloque "Documento".
   - Elegir un PDF o una foto real desde el selector nativo.
   - Esperar sin tocar nada más, al menos 2 segundos completos.
   - Mirar si aparece el cartel ámbar "No se abrió el selector de archivos..." **a pesar de que el
     documento sí quedó adjuntado** (la fila del archivo visible).
   - Si aparece: es un bug real y visible, con la causa ya diagnosticada y corregida en el código — sólo
     falta confirmar que corrige también en el teléfono. Si no aparece: se cierra sin tocar nada más.

2. **VAL-58, la recompresión de una foto de cámara real.** Los tests usan un JPEG armado con `canvas`
   en el propio navegador (911 KB → 188 KB), que es real pero no es una foto tomada con la cámara del
   teléfono ni pasada por su compresión nativa previa. Sacar una foto real de una tarjeta de embarque o
   voucher con la cámara del teléfono, adjuntarla, y confirmar que el aviso de "se guardó en menor
   calidad" aparece con números que tienen sentido y que el archivo se sigue viendo bien al descargarlo.

3. **VAL-59, con una tarjeta de embarque real.** Los tres casos (sin vuelo, un vuelo, varios vuelos)
   están probados con datos sintéticos que sí matchean como se espera. Confirmar con una tarjeta de
   embarque real del PM (foto o PDF) que el número de vuelo y la fecha se leen bien y que el matcheo
   contra un vuelo cargado a mano funciona con los datos reales, no sólo con los inventados para la
   prueba.

4. **VAL-60, si la plataforma de verdad manda un array de un solo lote.** El simulador de `sample.json()`
   está escrito contra el contrato documentado (`sample.d.ts` 0.2.41), pero **ningún modelo real
   contestó nunca** en este entorno el formato de lote que la app pide. Subir un lote de tres o cuatro
   documentos reales desde el teléfono y confirmar en la consola de red (o en los registros de
   `ValijaTier.log()` si están expuestos) que efectivamente hubo una sola llamada, no una por archivo.

5. **El tema oscuro y la tipografía en las pantallas nuevas**, en el visor real del teléfono. Los tests
   de navegador corrieron mayormente en claro; QA (C7) capturó una pantalla en oscuro y no encontró
   problemas de tokens, pero eso fue en el mismo sustituto de escritorio, no en el visor del teléfono
   ni con lector de pantalla — declarado como no cubierto tanto por QA como por esta auditoría.

6. **Que `58ca080` (si se decide llevarlo a producción junto con esto) diga la verdad en el teléfono
   también** — es documentación, no cambia el comportamiento de la app, así que no debería hacer falta
   probarlo en el teléfono, pero lo anoto para que quede completo: nada de la sección "Qué se entregó y
   qué no" del brief depende de ningún gesto, es texto para quien lea el contrato.

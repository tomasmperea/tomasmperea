# Auditoría — v19, causa raíz del archivo vacío (commit `388a6977e8115dd2dc1b00804b5c2df6e042886f`)

**Audita:** rol `auditor` · **Fecha:** 2026-09-17 · **Puntúa contra:** `docs/auditoria/rubrica.md`
**Árbol verificado quieto:** `git status --porcelain` vacío y `HEAD` en `388a6977e8115dd2dc1b00804b5c2df6e042886f`,
comprobado al empezar y otra vez al terminar de escribir este archivo.

## Veredicto: NO PUBLICAR — 75/100

El diagnóstico de causa raíz de este commit es, con distancia, el más sólido de las cuatro rondas: no
apuesta a una hipótesis sobre el Android del PM, identifica el defecto leyendo el código (una guarda con
`return` temprano que sólo podía producir la línea exacta que el PM mandó), lo reproduce con un sustituto
que instala el método real de la plataforma (`Blob.prototype.bytes()`, leído del contrato, no inventado) y
prueba con un control negativo que el arreglo lo elimina. Sobre esa parte no encontré nada que objetar.

Lo que impide publicar no es la ingeniería: son dos cosas chicas, baratas de arreglar, y del mismo tipo de
descuido que esta iteración dice estar corrigiendo:

1. **El commit y el documento para el PM afirman "10 FALLAS" y yo corrí el mismo arnés y conté 9**, no 10 —
   y de esas 9, sólo 8 están "en los dos gestos"; la novena es un tercer chequeo (tamaño/tope) que no es
   un gesto. Ninguna de las dos cifras se verificó antes de escribirla. Es el mismo patrón de exceso de
   confianza que el propio commit corrige en el mensaje de error de la app ("probé de tres formas
   distintas" cuando había probado una).
2. **`docs/briefs/adjuntar.md` no se tocó** y quedó citando `docs/qa/v18-como-lo-compruebo-en-el-telefono.md`
   — un archivo que este mismo commit renombró a `v19-...`. El brief, que es el contrato de la historia,
   apunta hoy a un archivo que no existe. Es la segunda vez que se pide esto (ya estaba en el veto de 68 y
   se cerró una vez en `8a3e8ef`) y vuelve a quedar desactualizado con el capítulo más importante de la
   saga: el que por fin encontró la causa.

Ninguna de las dos requiere tocar código ni repetir ningún arnés. Son ediciones de texto. No hay motivo
para gastar otra ronda del PM por esto, pero tampoco hay motivo para no arreglarlas antes de pedirle que
mire su teléfono por cuarta vez.

---

## 1 · ¿Es la causa raíz, o me volví a enamorar de una explicación? — No es una hipótesis más

Repliqué el argumento sin dar nada por sentado:

- Busqué todas las llamadas a `anotar(...)` en `app/parts/adjuntos-engine.js` y confirmé que **existe un
  solo lugar** que registra `via: "memoria"` (la guarda de `file.bytes`). Ningún otro código puede producir
  ese texto.
- En el código de la v18 (`3ecd99c`), esa rama hacía `return` inmediatamente después de anotar. Es la
  única rama de las cuatro (`memoria`, `arrayBuffer`, `slice`, `FileReader`) que corta el resto de la
  cadena. Por construcción, **la única forma de que el diagnóstico muestre un solo intento ("memoria") y
  nada más es que esa guarda haya entrado y cortado el resto.** No es una coincidencia compatible con la
  hipótesis: es la única explicación posible dado el código tal cual estaba escrito.
- Punto importante que el propio commit no remarca pero fortalece el argumento: **el arreglo no depende de
  acertar *por qué* `file.bytes` era verdadero en el teléfono del PM.** `bytesUtilizables` rechaza
  cualquier valor truthy que no sean bytes de verdad — sea la función nativa `Blob.prototype.bytes`, sea
  cualquier otra cosa que un navegador o un intermediario le cuelgue a `.bytes`. Si la causa exacta ("es el
  método nativo") resultara no ser la del teléfono del PM, el arreglo igual sostiene, porque ataca la clase
  del error (una guarda que confía en "verdadero" sin verificar el contenido) y no el caso puntual. Esto sí
  es la diferencia real con las tres rondas anteriores, cada una apostada a una hipótesis específica sobre
  el Android del PM que, si fallaba, se llevaba puesto todo el arreglo.

**No encontré una segunda hipótesis viva** que explique la misma línea y que este arreglo no cubra. Busqué
específicamente otro lugar del motor que compare por verdadero un nombre de método de `Blob` (`slice`,
`text`, `stream`, `arrayBuffer`) y no encontré ninguno: los usos existentes chequean `typeof x === "function"`
correctamente (`app/parts/adjuntos-engine.js:962,969`).

Conclusión de este punto: **dimensión 2 alta**, sin reservas sobre el método.

## 2 · El arnés `metodo-bytes-nativo.js` — leído del contrato, no de memoria, con un error de conteo

Confirmé que `Blob.prototype.bytes()` es una API real de la plataforma (Promise<Uint8Array>, añadida a los
navegadores Chromium recientes) y que el arnés la instala exactamente así, en `Blob.prototype`, sin tocar
la app (`app/pruebas/metodo-bytes-nativo.js:59-69`). Antes de usarla, el propio arnés verifica que el
sabotaje realmente llegó (`estado.enPrototipo`, `estado.esVerdadero`) — la lección que costó cuatro
escenarios en septiembre (closures que no sobreviven `addInitScript`) está aplicada acá.

**Control negativo, corrido por mí:**

```
git show 3ecd99c:app/valija.html > /tmp/v18.html
NODE_PATH=/opt/node22/lib/node_modules node app/pruebas/metodo-bytes-nativo.js /tmp/v18.html
```

Resultado real: **9 FALLARON**, no 10. Conteo exacto de mi corrida:

| # | Falla | ¿Es uno de "los dos gestos"? |
|---|---|---|
| 1-4 | gesto 1 (adjuntar): no dice que no pudo leer / muestra peso / línea `memoria` / reserva guardada con doc | Sí |
| 5-8 | gesto 2 (importar): cola no dice no pudo leer / `memoria N B` / Interpretar habilitado / doc de tarjeta adjunto | Sí |
| 9 | "el chico...ya no vuelve vacío por `memoria`" (sección de tamaño/tope) | **No** — es un tercer chequeo con `AdjuntosEngine.prepararDocumento` directo, no un gesto de UI |

El script mismo lo dice bien en su propio resumen (`9 FALLARON`); el error está en la prosa humana del
commit y del documento para el PM, que dicen "10 FALLAS...en los dos gestos" sin haber vuelto a correr el
número antes de escribirlo. Repetí la corrida dos veces más para descartar que fuera un problema de mi
entorno: siempre 9.

**Contra la v19 actual**, corrido por mí: todo en verde, incluida la sección que separa la explicación del
tamaño de la del origen (`chico` entra por el lector, `grande` se va por `rutaGrande` sin tocar la guarda).

Este es un hallazgo real, no cosmético: es exactamente el tipo de cifra que un PM cansado de cuatro rondas
va a citar de vuelta ("dijiste que fallaba 10 veces"), y si alguna vez alguien vuelve a correr el arnés y
cuenta 9, la primera pregunta legítima va a ser si el resto de las cifras del reporte también se
redondearon para arriba. No cambia la conclusión causal, pero sí baja la honestidad de lo verificado.

## 3 · El arreglo — correcto, y barrido completo confirmado

Diff verificado línea por línea (`git show 388a697 -- app/parts/adjuntos-engine.js`, `-- app/valija.html`).
Los dos cambios que el commit promete son exactamente los que están:

- `bytesUtilizables(x)` rechaza `!x`, funciones, y cualquier resultado de largo cero — incluida la ruta
  hostil (`new Uint8Array(x)` con un getter que lanza) protegida con `try/catch`, que es justo el caso que
  la auditoría anterior había encontrado suelto en `preparar-no-lanza.js`. Lo corrí: sigue en verde,
  incluida la parte D ("ningún gesto llega al motor sin pasar por la función blindada").
- La guarda ya no hace `return` si `file.bytes` no son bytes de verdad: anota `"no-son-bytes"` y sigue a los
  tres caminos reales.
- El mismo filtro se aplicó a `tamañoDe`, donde `.length` de una función es su aridad.

**Barrido de la clase, repetido por mi cuenta:** `grep -n "\.bytes\b"` en `app/valija.html` y
`app/parts/*.js` no muestra ninguna otra comparación por verdadero sobre un nombre de método de `Blob`
(`slice`, `text`, `stream`, `arrayBuffer` están todos chequeados con `typeof ... === "function"`). No se
escapó ninguno de los que busqué. Confirmo el "dos lugares (parts + embebido) más `tamañoDe`" del commit:
son exactamente los mismos dos cambios repetidos en las dos copias, ni más ni menos.

**Motor embebido idéntico al fuente — verificado byte a byte, no por longitud aproximada:** extraje el
cuerpo del IIFE de `AdjuntosEngine` en `app/valija.html` y el cuerpo de la factory UMD en
`app/parts/adjuntos-engine.js` con el mismo método de anclas que usa `motores-desde-html.js`, y
`diff`eé ambos: **51.262 caracteres, idénticos**, sin ninguna línea distinta (la única diferencia que
reporta `diff` es un salto de línea final, un artefacto de dónde corté el archivo, no del contenido).

## 4 · Los gestos contra el reporte — dos cubiertos, un tercero cubierto por invariante estructural

El PM vio el síntoma por **importar** (captura 1) y por **adjuntar a una reserva** (capturas 2 y 3).
Ambos tienen prueba propia bajo la condición causal reproducida (`metodo-bytes-nativo.js`, gestos 1 y 2,
verificados por mí, en verde).

Busqué un tercer camino de entrada de archivo, como pide el brief, y encontré uno real: **"Reemplazar"**
(`data-pick="doc_repl"`, `app/valija.html:8542-8543`), que cambia el documento de una reserva ya guardada.
Esto **no** tiene un escenario propio dentro de `metodo-bytes-nativo.js` bajo la condición del
`bytes()` nativo. Es exactamente el patrón que costó una iteración con el importador ("comparten el motor"
no alcanza si el bug vive arriba del motor compartido).

Pero acá la situación es distinta y la verifiqué en vez de asumirla: `reemplazar()` llama a
`prepararAdjunto` de forma directa, sin lógica propia intermedia (`app/valija.html:8599-8604`), y
`app/pruebas/preparar-no-lanza.js` — que sí corrí — prueba una invariante estructural más fuerte que un
escenario por gesto: que existe **exactamente una** llamada a `AdjuntosEngine.prepararDocumento` en toda la
app, y que está dentro de la cadena de promesas blindada. La corrí:

```
NODE_PATH=/opt/node22/lib/node_modules node app/pruebas/preparar-no-lanza.js "$(pwd)/app/valija.html"
```

Resultado: verde, con `hay exactamente UNA, la de prepararAdjunto (1): si aparece otra, este caso falla`.
Esto demuestra por lectura del código servido —no por confianza— que `doc_repl` pasa por el mismo código
exacto que los dos gestos que sí se sabotearon. No es una prueba directa de `doc_repl` bajo la condición
del `bytes()` nativo, pero es una garantía estructural razonable, ya identificada y resuelta así hace dos
rondas (documentado en el propio `docs/briefs/adjuntar.md`, sección "Cierre de la iteración 3"). No lo
puntúo como hallazgo bloqueante nuevo; lo dejo dicho porque el brief pedido explícitamente sobre este punto
sigue siendo la única defensa escrita, y esa defensa no se repitió en el commit de hoy.

## 5 · El documento del PM (`docs/qa/v19-como-lo-compruebo-en-el-telefono.md`)

La sección "Esta vez sí sé qué estaba roto" es honesta en lo sustantivo: separa con claridad qué se
reprodujo acá (`Blob.prototype.bytes()` instalado, arnés en rojo con el texto exacto que el PM vio, verde
con el arreglo) de lo que sigue sin poder verificarse (que alcance en el teléfono real, que no haya una
segunda causa). No encontré ninguna frase que prometa "andá tranquilo, ya está" — al contrario, dice
explícitamente "puede haber más de una cosa rota". Eso es exactamente lo que la rúbrica pide premiar.

El cartel de Android ("Memoria insuficiente...") está tratado correctamente: declarado aparte, sin
conclusión, pidiendo el dato de vuelta en vez de inventar una causa. No lo trataría como un segundo defecto
abierto todavía — es un cartel del sistema operativo, no de la app, y no hay ninguna señal de que la app lo
haya provocado; tratarlo como bloqueante ahora sería inventar una causa en la dirección contraria (la
misma regla 4/5 de `CLAUDE.md`, aplicada al revés). Está bien dimensionado.

**El único problema real de este documento es el mismo del punto 2:** repite textualmente "el arnés falla
10 veces...en los dos gestos", que verifiqué que es falso (9, y una de ellas fuera de los gestos). Es una
frase que el PM puede citar de vuelta, y está mal.

## 6 · El cartel "Memoria insuficiente" de Android

Coincido con el tratamiento del documento: no hay evidencia de que sea la app la que lo dispara, no se
puede investigar desde acá, y no corresponde ni cerrarlo como "no es nada" ni abrirlo como defecto formal
todavía. Pedir el dato de vuelta (si reaparece, avisar) es la respuesta proporcional. No lo sobredimensioné
ni lo bajé de categoría.

---

## Tabla de las seis dimensiones

| # | Dimensión | Peso | Puntaje | Por qué |
|---|---|---|---|---|
| 1 | Verificación en el entorno real | 30 | **20** | Sustituto fiel: gestos reales (`click`, `touchscreen.tap`, `waitForEvent("filechooser")`, nunca un `change` disparado a mano), `File`/`Blob` reales, y el método nativo que causó el defecto instalado leyendo el contrato de la plataforma, no de memoria — con verificación explícita de que el sabotaje se aplicó. La brecha (el teléfono real del PM) está declarada con precisión y con guion accionable. Se queda en el piso del bucket de "sustituto fiel" porque sigue sin haber ninguna pisada real de teléfono, que es la única cosa que esta dimensión no puede dar por sentada aunque el sustituto sea excelente — es la misma confianza que las tres rondas anteriores también tenían en su propio sustituto. |
| 2 | Diagnóstico de causa raíz | 15 | **14** | Reproducido, causa identificada por lectura de código con un argumento de unicidad verificable (un solo lugar puede producir la línea observada), arreglo probado con control negativo real que lo elimina, y el arreglo ataca la clase del error (no depende de acertar por qué `.bytes` era verdadero). Resta 1 punto porque el propio arnés que sostiene esta conclusión se citó mal (9 vs. 10) sin volver a correrlo antes de publicar el número. |
| 3 | Honestidad de lo verificado | 15 | **11** | Separación clara y explícita de verificado / no comprobado, sin que haga falta pedirlo (secciones dedicadas, ninguna promesa de "andá tranquilo"). Resta por una afirmación cuantitativa falsa ("10 FALLAS...en los dos gestos") que llegó igual al commit y al documento que lee el PM, sin haberse vuelto a verificar — el mismo tipo de exceso de confianza que este commit corrige en el mensaje de error de la app. |
| 4 | Cumplimiento del contrato | 15 | **9** | El motor y sus pruebas cumplen VAL-58 sin regresiones. Pero `docs/briefs/adjuntar.md` —el contrato de esta historia— no se tocó y hoy cita `docs/qa/v18-como-lo-compruebo-en-el-telefono.md`, un archivo que este mismo commit renombró: el brief apunta a algo que ya no existe. Es el mismo pedido de la ronda anterior (cerrado una vez en `8a3e8ef`) reabierto con el capítulo más importante de la saga sin incorporar. |
| 5 | Calidad interna | 15 | **12** | Pruebas verificadas por mí, corriendo 20 arneses, todas en verde y probando lo que dicen probar (gestos reales, control negativo con sabotaje confirmado). Motor embebido idéntico byte a byte al de `app/parts/`. `doc_repl` cubierto por una invariante estructural razonable en vez de duplicar un escenario. Resta por la desactualización del brief y por el desajuste de conteo del arnés nuevo, que es exactamente el patrón "la documentación no dice lo que el código/las pruebas hacen" que este proyecto ya pagó una vez. |
| 6 | Diseño y decisiones de producto | 10 | **9** | Decisiones bien argumentadas y con su porqué: no cortar el camino en la guarda, tratar cero bytes como "no sé" y no como "vacío", mantener el mensaje honesto sobre cuántos caminos probó, resolver `doc_repl` con una propiedad estructural en vez de un escenario más. Ninguna decisión contradice el brief a escondidas. |
| | **Total** | **100** | **75** | |

**Diferencia con la autoevaluación:** no vi una autopuntuación separada para comparar (no se me proveyó
una tabla de autoevaluación de quien entregó este commit). Si existe en otro lado, corresponde anotar la
diferencia según la regla de la rúbrica.

---

## Afirmaciones verificadas, con comando y resultado

| Afirmación | Cómo la verifiqué | Resultado |
|---|---|---|
| "17 arneses en verde" | Corrí los 17 nombrados más `auditoria-h1`/`h2`, uno por uno, con `NODE_PATH=/opt/node22/lib/node_modules` | **Cierto**, sin ninguno en rojo, y los conteos por arnés coinciden: `valija-bloque-b` 111, `adjuntar-documento` 81, `tarjeta-y-lote` 77, `importar-arranque` 59, `importar-sin-imagenes` 45, `tier-del-modelo` 42, `motores-desde-html` 39, `hallazgos-qa-bloque-b` 31, cruces 4+4+5, y `base-compartida`/`lote-modelo-real`/`archivo-del-correo`/`archivo-sin-tamano`/`archivo-de-una-lectura`/`tres-caminos-de-lectura`/`preparar-no-lanza`/`importar-botones` sin fallas |
| "`metodo-bytes-nativo.js` contra la v18 da 10 FALLAS en los dos gestos" | Corrí el arnés contra `git show 3ecd99c:app/valija.html`, tres veces | **Falso**: da **9**, y una de las nueve (la del tamaño/tope) no es parte de "los dos gestos" — es un tercer chequeo directo sobre el motor |
| "Con el arreglo, verde" (mismo arnés, contra la v19 actual) | Corrí `metodo-bytes-nativo.js` sin argumento (usa `app/valija.html` por defecto) | **Cierto**, `Todo en verde`, incluida la comprobación de que el método nativo se llamó 0 veces (la app lo ignora) |
| "Motor embebido idéntico al de `app/parts/adjuntos-engine.js`" | Extraje ambos cuerpos con el mismo método de anclas que `motores-desde-html.js` y los comparé con `diff` | **Cierto**, 51.262 caracteres, idénticos byte a byte |
| "Barrido de la clase: dos lugares más `tamañoDe`, los otros motores llaman `arrayBuffer()` bien" | `grep -n "\.bytes\b\|\.slice\b\|\.text\b\|\.stream\b\|\.arrayBuffer\b"` en `app/valija.html` y `app/parts/*.js` | **Cierto**, no encontré ninguna otra guarda que compare por verdadero un nombre de método de `Blob` |
| "`VALIJA_VERSION` subió a 19" | `git show 388a697 -- app/valija.html` | **Cierto** |
| "`doc_repl` cubierto por la invariante de llamada única" | Corrí `preparar-no-lanza.js` contra `app/valija.html` | **Cierto**, `hay exactamente UNA, la de prepararAdjunto (1)` |
| "El árbol se mantuvo quieto" | `git status --porcelain` y `git rev-parse HEAD`, al empezar y al terminar | **Cierto**, sin cambios, siempre en `388a697` |
| "`docs/briefs/adjuntar.md` refleja esta saga" | Leí el archivo completo | **Falso**: cita `docs/qa/v18-como-lo-compruebo-en-el-telefono.md`, que este commit renombró a `v19-...`; no menciona la causa raíz encontrada hoy |
| "El cartel de Android está declarado sin inventar una causa" | Leí `docs/qa/v19-como-lo-compruebo-en-el-telefono.md` completo | **Cierto** |

---

## Qué falta para llegar a 85, en orden

1. **Corregir la cifra "10 FALLAS...en los dos gestos"** en el mensaje de commit (si se vuelve a escribir
   en algún lado) y, más importante, en `docs/qa/v19-como-lo-compruebo-en-el-telefono.md`: son 9, y 8 de
   ellas están en los dos gestos; la novena es el chequeo de tamaño. Volver a correr el arnés antes de citar
   un número, no volver a escribirlo de memoria.
2. **Actualizar `docs/briefs/adjuntar.md`**: corregir la referencia a `v18-como-lo-compruebo-en-el-telefono.md`
   (ya no existe, es `v19-...`) y agregar el capítulo de hoy — la causa raíz encontrada, cómo se reprodujo,
   y que sigue pendiente de la confirmación del PM. Es la segunda vez que se pide esto.
3. Con esas dos correcciones (ambas de texto, sin tocar código ni volver a correr arneses de ingeniería),
   esta entrega pasa el piso de 85 y queda lista para publicarse **como candidato** — no como iteración
   terminada, porque eso todavía depende del punto siguiente.

---

## Lo que nadie puede verificar desde acá

Nada de esto es nuevo respecto de las rondas anteriores en el fondo, pero esta vez hay algo concreto para
mirar en la pantalla, no sólo "probá de nuevo":

1. **Que la versión publicada sea realmente la `v19`.** Antes de pedirle nada al PM, quien publique tiene
   que leer el archivo que sirve el Artifact y buscar adentro la cadena `const VALIJA_VERSION = "19"` y el
   comentario de `bytesUtilizables` — si no están, no está publicado, sin importar qué diga el último commit
   (la lección de `3ecd99c`, ya escrita en `CLAUDE.md`).
2. **Prueba 1 — adjuntar el PDF del mail a una reserva** (el gesto de las capturas 2 y 3): tocar una
   reserva, bloque DOCUMENTO, botón "Archivo", elegir el PDF real que llegó por mail. Si funciona, aparece
   la fila con su peso real. Si falla, aparece la línea gris de diagnóstico completa — esa línea, en una
   captura que entre entera, es el dato que hace falta.
3. **Prueba 2 — el mismo PDF, importándolo** (el gesto de la captura 1): dentro del viaje, "Importar" →
   "Archivo" → el mismo PDF. Si funciona, interpreta la tarjeta y deja el documento adjunto. Si falla, la
   misma línea gris, y ya no puede decir "es un escaneo" sobre un archivo que no se pudo leer.
4. **Prueba 3 — que la foto de la cámara siga entrando y reduciéndose sola** (2,7 MB → ~112 KB), para
   confirmar que el arreglo de la guarda no rompió el camino que sí andaba.
5. **Si aparece de nuevo el cartel de Android "Memoria insuficiente para completar la operación anterior"**,
   avisar con esas palabras exactas y, si es posible, en qué paso apareció. Nadie desde acá puede determinar
   si es un problema real o ruido del sistema operativo — sólo el teléfono del PM lo puede decir.
6. **El escenario `doc_repl` bajo la condición exacta del `bytes()` nativo** no se corrió (sólo se probó su
   invariante estructural). Si alguna vez se quiere cerrar esto sin depender de esa inferencia, agregar un
   tercer gesto a `metodo-bytes-nativo.js` que reemplace un documento ya adjunto es barato y ya está
   identificado.
